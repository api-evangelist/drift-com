openapi: 3.2.0
info:
  title: Drift Playbooks API
  description: Drift is a cloud-based solution designed to help businesses automate sales processes by adding live chat windows to websites for increased customer experience and loyalty. Key features include chatbots, geo-targeting, offline access, proactive chating and transfers/routing.
  version: 1.0.0
servers:
- url: http://{{base_url}}
- url: https://driftapi.com
security:
- bearerAuth: []
tags:
- name: Playbooks
  description: https://devdocs.drift.com/docs/playbook-model-1
paths:
  /playbooks/list:
    get:
      tags:
      - Playbooks
      summary: Drift List playbooks
      responses:
        '200':
          description: Successful response
          content:
            application/json: {}
      operationId: getPlaybooksList
      x-operation-id-source: derived
  /playbooks/clp:
    get:
      tags:
      - Playbooks
      summary: Drift List conversational landing pages (CLP)
      responses:
        '200':
          description: Successful response
          content:
            application/json: {}
      operationId: getPlaybooksClp
      x-operation-id-source: derived
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
